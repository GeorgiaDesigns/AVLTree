#include <iostream>
#include "Tree.h"

int main() {

    Tree<int>* arvoreA = new Tree<int>();

    arvoreA->insert(1);
    arvoreA->insert(2);
    arvoreA->insert(3);
    arvoreA->insert(4);
    arvoreA->insert(5);

}